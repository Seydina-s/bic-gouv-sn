# ERREURS.md — Bic Gouv SN

Journal des erreurs corrigées dans ce projet (CLAUDE.md §16). Une erreur n'est close que lorsque son correctif **et** sa règle de prévention sont documentés.

| Date | Ce qui s'est passé | Cause racine | Correctif | Fichier(s) | Règle de prévention | Généralisable ? |
|---|---|---|---|---|---|---|
| 24/09/2026 | Un chemin Windows écrit dans STATUS.md via `sed`/`awk` a été corrompu (`\t`, `\n`, `\U` interprétés comme séquences d'échappement). | Insertion de chemins contenant des antislashs par des outils qui interprètent les échappements. | Ligne réécrite avec l'outil d'écriture de fichier (sans interprétation). | `.claude/session/STATUS.md` | Tout texte contenant des chemins Windows est écrit avec Write/Edit ou un heredoc quoté (`<<'EOF'`), jamais via `sed`/`awk`/`echo -e`. Relire la ligne après écriture. | Oui |
| 24/09/2026 | La validation d'une URL mal formée faisait planter le programme (exception) au lieu de la refuser proprement. | Zod 4 exécute les règles personnalisées même quand le contrôle de format d'URL a déjà échoué ; `new URL()` lève alors une exception. | `isOfficialSourceUrl` protégée par try/catch (et non `URL.parse`, absent de Hermes sur mobile). | `packages/shared-types/src/common/official-source.schema.ts` ; test anti-régression `official-source.schema.test.ts` (cas « not a url ») | Toute fonction utilisée dans un `refine`/`superRefine` Zod doit être totale : elle ne lève jamais d'exception, quelle que soit l'entrée. Toujours tester une entrée invalide. | Oui |
| 24/09/2026 | Tests mobiles en échec à la mise en place (9/9 puis 2/9). | 1) RNTL 14 rend les composants de façon asynchrone (`await render`). 2) Le preset jest de React Native remplace `useColorScheme` par un bouchon qui renvoie toujours « light » : le basculement clair/sombre n'était pas réellement testé. 3) TypeScript 6 ne charge plus automatiquement les `@types` installés. | `await` partout ; vrai `useColorScheme` restauré et module Appearance simulé ; `"types": ["jest"]`. Test de mutation : casser le suivi du thème fait bien échouer 2 tests. | `apps/mobile/src/theme/ThemeProvider.test.tsx`, `apps/mobile/tsconfig.json` | Vérifier qu'un test peut échouer (mutation) avant de lui faire confiance ; lire les bouchons du preset de test pour toute API système testée. | Oui |
| 24/09/2026 | Test API en échec intermittent pendant le contrôle complet (12,6 s > délai de 5 s), mais réussi seul (2,4 s). | Les packages sont testés 4 à la fois : sous charge, l'import à froid de Fastify + Swagger dépassait le délai par défaut de Vitest. | `testTimeout: 30_000` dans `vitest.shared.ts` ; contrôle complet stable sur 2 exécutions. | `vitest.shared.ts` | Fixer un délai de test adapté aux machines lentes et aux petits runners de CI ; relancer un contrôle complet au moins 2 fois avant de conclure. | Oui |
| 24/09/2026 | Prettier a reformaté les fichiers de la skill Impeccable (code tiers) et ESLint les a analysés. | `.claude/` n'était pas exclu des outils de qualité. | `.claude` ajouté à `.prettierignore` et aux exclusions ESLint ; Impeccable réinstallé (`--force`) pour restaurer ses fichiers d'origine. | `.prettierignore`, `eslint.config.mjs` | Exclure tout code tiers des outils de formatage et d'analyse dès son installation. | Oui |
| 24/09/2026 | Premier passage CI : le job de scan de secrets échoue (403 « Resource not accessible by integration »). | gitleaks-action lit la liste des commits de la pull request ; les permissions minimales du workflow (`contents: read`) ne l'autorisaient pas. | `pull-requests: read` accordé au seul job de sécurité. CI verte (3/3). | `.github/workflows/ci.yml` | Pour chaque action tierce, vérifier dans sa doc les permissions exactes nécessaires, puis les accorder au niveau du job, jamais globalement. | Oui |
| 25/09/2026 | Groupe de libellés `categories` pris pour un message pluriel : ses clés n'étaient pas reconnues. | Une clé `other` (« Actualité ») dans le groupe suffisait à le faire passer pour un message pluriel, dont la détection ne vérifiait que la présence de `other`. | Clé renommée `general` ; détection stricte : un message pluriel ne contient **que** `one`/`many`/`other`. | `packages/i18n/src/catalog.ts` ; test « does not mistake a label group… » dans `catalog.test.ts` | Toute détection de forme par « canard » doit vérifier l'ensemble exact des clés, pas seulement une clé caractéristique ; test avec un objet piège. | Oui |
| 25/09/2026 | Libellés de la barre d'onglets tronqués (moitié inférieure coupée). | L'icône occupait la hauteur disponible et le libellé était comprimé à 9 px pour une ligne de 16 px. Diagnostic retardé : le serveur d'aperçu web servait un paquet périmé (morceaux « lazy » non rechargés). | Hauteur de barre explicite (64 dp + zone système) et `flexShrink: 0` sur le libellé ; vérification sur un export web statique reconstruit. | `apps/mobile/src/app/(tabs)/_layout.tsx` | Pour vérifier un correctif visuel, toujours reconstruire un export propre (pas le serveur de dev) ; mesurer la géométrie réelle (hauteur calculée) au lieu de deviner. | Oui |
| 25/09/2026 | Client réseau de l'app : `fetch` capturé au chargement du module. | Paramètre par défaut `fetchImpl = fetch` évalué une seule fois : un `fetch` remplacé ensuite (tests, intercepteur) était ignoré. | Résolution de `fetch` à chaque appel. | `apps/mobile/src/api/news-client.ts` | Ne jamais capturer une globale remplaçable dans une valeur par défaut évaluée au chargement : la lire au moment de l'appel. | Oui |
| 25/09/2026 | Après l'ajout des photos, l'app plantait au démarrage (« Cannot read properties of undefined (reading 'sources') ») quand elle avait déjà des actualités en cache. | Le cache hors ligne enregistré par l'ancienne version (sans champ `cover`) était réaffiché tel quel par la nouvelle, sans revalidation : le format des données avait changé mais le cache ne le savait pas. | Empreinte automatique du contrat d'API (`apiContractFingerprint`, calculée depuis les schémas Zod partagés) utilisée comme `buster` du cache persistant : tout cache d'un autre format est jeté et rechargé. Test de mutation : sans le correctif, le test reproduit exactement le plantage. | `packages/shared-types/src/api/contract.ts`, `apps/mobile/src/data/QueryProvider.tsx` ; test « discards news saved in an older data format » dans `app-shell.test.tsx` | Tout cache persistant (téléphone, CDN, disque) doit être versionné par une empreinte **dérivée automatiquement** du format des données, jamais par un numéro à incrémenter à la main. Chaque changement de schéma d'API se teste avec un cache de l'ancien format. | Oui |
| 25/09/2026 | Une photo attachée à un article ne changeait pas l'ETag de l'API : les téléphones auraient gardé la version sans photo. | L'ETag était calculé sur l'empreinte du texte (`contentHash`) ; la photo est ajoutée après, sans modifier cette empreinte (volontairement, pour ne pas créer de fausse « nouvelle version » de l'article). | Clé de fraîcheur = empreinte du texte + clé de la photo (`freshnessKey`). | `apps/api/src/news/present.ts` ; test « has no cover until the ingestion attaches one, then changes the ETag » | Un ETag doit couvrir **tout** ce que la réponse contient, y compris les données enrichies après coup. | Oui |
| 25/09/2026 | Un commit a été créé et poussé alors que le contrôle complet signalait une erreur de lint. | Commande enchaînée avec `;` (et un filtre `grep` qui masquait le code de sortie) : le commit s'exécutait quel que soit le résultat du contrôle. | Correctif de lint dans un commit de suite ; la CI de la pull request bloquait de toute façon la fusion. | `apps/mobile/src/app-shell.test.tsx` | Ne jamais enchaîner commit/push après un contrôle sans condition stricte : `pnpm check && git commit`, sans filtre qui avale le code de sortie. | Oui |
| 25/09/2026 | Sur l'iPhone de l'utilisateur, l'app affichait « Hors ligne » alors que l'API répondait : la version installée refusait les réponses contenant le nouveau champ `cover`. | Schémas de réponse stricts (`z.strictObject`) utilisés aussi côté app : tout ajout à l'API cassait les versions déjà installées (qui resteront des mois sur les téléphones). | Schémas de réponse non stricts (champs inconnus ignorés) + lecteur tolérant (`readNewsList`/`readNewsDetail`) : blocs et formats d'image inconnus écartés, article illisible sauté au lieu de faire échouer la page. | `packages/shared-types/src/api/tolerant-reader.ts`, `apps/mobile/src/api/news-client.ts` ; tests `tolerant-reader.test.ts` et « still reads a newer API… » | Contrat d'API : évolutions additives uniquement ; un client ne doit jamais valider une réponse en mode strict. Tester chaque client contre une réponse « plus récente » (champ, bloc et format inconnus). | Oui |
| 25/09/2026 | Malgré la demande explicite de l'utilisateur (tout rédiger en français), Claude a continué à lui écrire en anglais (points d'étape et message de synthèse). | Contexte de travail majoritairement anglais (code, commits, sorties d'outils, instructions de skills) qui a « déteint » sur les messages ; la consigne avait été enregistrée en mémoire mais pas appliquée message par message dans la session en cours. | Excuses, explication, récapitulatif refait en français ; règle ajoutée ci-contre et dans ERREURS-GLOBALES.md. | Messages à l'utilisateur (pas de fichier) | Avant d'envoyer **chaque** message visible par l'utilisateur, vérifier sa langue : français, sans exception (y compris les notes courtes entre deux actions). Seuls le code, les commits et les commentaires techniques restent en anglais. | Oui |
| 25/09/2026 | Test API en échec pendant le contrôle complet : « Hook timed out in 10000ms » dans la préparation (`beforeEach`), machine chargée (serveurs de développement actifs). | Le correctif du 24/09 n'avait relevé que le délai des tests (`testTimeout`), pas celui des étapes de préparation (`hookTimeout`, 10 s par défaut) où Fastify est importé à froid. | `hookTimeout: 30_000` dans `vitest.shared.ts`. | `vitest.shared.ts` | Quand on relève un délai pour cause de charge, relever **tous** les délais concernés (tests et préparations) ; un correctif partiel d'une même cause racine est une récidive annoncée. | Oui |
| 25/09/2026 | Récidive : textes contenant des accents graves ou `$` écrits via `node -e` dans le shell, donc tronqués (STATUS.md, Selvage.tsx, spa-serve). | La règle du 24/09 visait les chemins Windows ; elle n'a pas été généralisée à tous les caractères spéciaux du shell (accent grave, `$`, antislash), et la facilité du `node -e` l'a emporté. | Fichiers réécrits entièrement avec l'éditeur. | `.claude/session/STATUS.md`, `apps/mobile/src/features/news/Selvage.tsx` | Tout contenu contenant un accent grave, `$` ou un antislash s'écrit **uniquement** avec l'outil d'écriture ou d'édition de fichiers ; le shell sert à exécuter, jamais à composer du texte. | Oui |
| 25/09/2026 | **Perte du fichier des articles** : après l'interruption brutale de la session (arrêt pendant l'import des images), `.data/news.json` gardait sa taille (7,5 Mo) mais ne contenait plus que des zéros ; 11 fichiers photo dans le même état. Contenus reconstruits depuis presidence.sn, photos existantes réutilisées. | Écriture « atomique » incomplète : fichier temporaire puis renommage, **sans forcer l'écriture sur disque (fsync)**. Après une coupure, le système peut garder le renommage mais pas le contenu. Aucune copie de secours. | Stockage des articles : écriture forcée sur disque avant renommage + copie de secours `.bak` tenue à jour et relue automatiquement si le fichier principal est illisible. Stockage des médias : même écriture durable. Contrôle des médias existants (fichiers vides ou nuls supprimés puis régénérés). | `packages/content-store/src/file-article-repository.ts`, `services/ingestion/src/media/media-storage.ts` ; test « survives a main file zeroed by a power cut » | Toute écriture de données sur disque : fichier temporaire → **fsync** → renommage, et une copie de secours ; toute reprise après incident commence par un contrôle d'intégrité des fichiers existants avant de les réutiliser. En production : PostgreSQL + sauvegardes (DATA-01). | Oui |
| 26/09/2026 | CI rouge sur la PR #17 : l'analyse des secrets (gitleaks) a pris pour une clé d'API la constante qui nomme l'emplacement de stockage des favoris (nom terminé par KEY). Faux positif ; la protection de `main` a bien empêché la fusion. | Le mot `KEY` dans le nom d'une constante qui n'est pas un secret (nom d'un emplacement de stockage) déclenche la règle générique « generic-api-key ». | Constante renommée `FAVORITES_SLOT`, commentaire « not a secret » ; aucune exception ajoutée au scanner (elle pourrait laisser passer un vrai secret). | `apps/mobile/src/features/favorites/favorites-store.ts` | Ne pas nommer `*_KEY`, `*_TOKEN`, `*_SECRET` ce qui n'est pas un secret ; lancer `gitleaks dir` en local avant de pousser un nouveau fichier. Ne jamais contourner le scanner par une liste d'exceptions. | Oui |
