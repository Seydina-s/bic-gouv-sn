# decisions.md — Bic Gouv SN

Journal des décisions structurantes. Une décision est consignée **avant** d'être appliquée. Aucune ligne n'est supprimée : une décision remplacée reste visible, avec un renvoi vers celle qui la remplace.

| Date | Décision | Auteur | Justification | Impact |
|---|---|---|---|---|
| 24/09/2026 | Articles en wolof : on ingère la version officielle `presidence.sn/wo/` ; on ne traduit que les éléments manquants (article WO absent ou en retard, fiches e-senegal.sn, interface, annexes), étiquetés « traduction automatique » jusqu'à relecture. | Utilisateur | Exactitude : la version officielle prime sur toute traduction | Pipeline d'ingestion FR + WO avec liaison FR ↔ WO ; traduction limitée aux trous |
| 24/09/2026 | Thème : l'app suit automatiquement et en direct le mode clair/sombre du téléphone ; override Clair / Sombre / Système dans les Réglages (défaut : Système). | Utilisateur | Confort et accessibilité | Tokens sémantiques obligatoires pour les deux thèmes, aucune couleur en dur |
| 24/09/2026 | Icônes : Phosphor Icons (ou mieux, sur justification). | Utilisateur | Pack élégant et cohérent | Composant `Icon` unique |
| 24/09/2026 | Git : aucune mention de Claude comme co-auteur, ni dans les commits, ni dans les PR, tags ou fichiers. Identité Git de l'utilisateur uniquement. | Utilisateur | Choix de l'utilisateur (CLAUDE.md §15) | Pas de ligne `Co-Authored-By` ni « Generated with Claude Code » |
| 24/09/2026 | Police de départ relevée dans la maquette : **Bricolage Grotesque** (titres, 600/800) + **Manrope** (texte, 400–700). Les deux sont sous licence OFL. Comparaison avec 2–3 alternatives (glyphes wolof, petites tailles, poids) prévue en Phase 1 Design ; choix final par l'utilisateur. | Claude (relevé factuel) | CLAUDE.md §4.3 demande de relever la famille exacte | Point de départ du design system typographique ; décision finale en attente |
| 24/09/2026 | Écart constaté : la maquette utilise des verts/jaunes/rouges différents de la charte (`#00733A`, `#F7C600`, `#C8102E`). **La charte de CLAUDE.md prévaut** (`#00853F`, `#FDEF42`, `#E31B23`). | Claude (application de CLAUDE.md) | La maquette est une inspiration, la charte est la règle | Tokens construits sur les couleurs de la charte |
| 24/09/2026 | Emplacement du projet conservé à `C:\bic-gouv-sn` (plutôt que sous `C:\Users\HP`). | Utilisateur (question) / Claude (analyse) | Monorepo React Native/Expo + Android (Gradle/CMake) sensible à la longueur des chemins sous Windows : un chemin racine court évite des échecs de build. Hors du profil utilisateur, pas de risque de synchronisation OneDrive sur `node_modules`. | Aucun déplacement ; chemins courts pour tout le dépôt |
| 24/09/2026 | Stack §4.1 validé par l'utilisateur, sous réserve qu'il soit le plus adapté au projet. Revue de Claude : adapté (coût quasi nul à l'échelle grâce à MapLibre/tuiles auto-hébergées et contenu servi par CDN ; une seule langue TypeScript sur mobile, API et admin). Ajustement recommandé : Node 22 LTS au lieu de Node 20 (fin de support avril 2026). Choix Meilisearch vs Typesense reporté à la Phase 2 (recherche). | Utilisateur | Validation explicite demandée par CLAUDE.md §4 | Le développement du Sprint 0 peut démarrer |
| 24/09/2026 | Dépôt GitHub **privé** sous le compte `seydina-s`. | Utilisateur | Code non public tant que le projet n'est pas lancé | CI GitHub Actions possible (F-10) |
| 24/09/2026 | Sentry : offre gratuite au démarrage, passage à une offre payante seulement si nécessaire et après validation. | Utilisateur | Coût minimal | F-12 débloqué dès création du compte |
| 24/09/2026 | Installation de pnpm autorisée. | Utilisateur | Gestionnaire du monorepo (§4.2) | F-02 débloqué |
| 24/09/2026 | Section « Opportunités » (financements, concours, marchés publics) **conservée** dans le périmètre. Contenu et sources officielles à définir plus tard ; rien ne sera affiché sans source officielle validée. | Utilisateur | Présente dans la maquette | Nouvelle entrée de backlog, priorité à fixer |
| 24/09/2026 | Statut : projet porté par le BIC Gouv ; autorisation officielle de la Présidence attendue prochainement. La preuve écrite reste à fournir avant toute soumission aux stores. | Utilisateur | Exigence Google Play / App Store pour une app gouvernementale | Blocage stores levé à réception du document |
| 24/09/2026 | Hébergement : pas de budget fixé ; pris en charge par l'organisme ; choix au meilleur rapport qualité/prix au moment venu (S1-02). | Utilisateur | — | Chiffrage comparatif à présenter avant tout engagement |
| 24/09/2026 | **Node 24 LTS** (au lieu de Node 22 envisagé) pour ce projet uniquement, installé via **fnm** dans `C:\Users\HP\tools\fnm` et fixé par `.node-version`. Node 20 système **conservé** comme version par défaut de la machine. | Utilisateur (condition : aucun risque) / Claude (analyse) | React Native 0.87 / Expo SDK 57 exigent Node ≥ 22.13 (Node 20 exclu). Node 22 finit en avril 2027, Node 24 en avril 2028. L'isolement par projet évite les complications vécues sur un ancien projet : les autres projets et outils globaux (claude-mem, firebase-tools) restent sur Node 20. | Toute commande du projet passe par `fnm exec --using=24` ; aucun changement pour le reste de la machine |
| 24/09/2026 | GitHub CLI 2.101 installé dans `C:\Users\HP\tools\gh` (PATH utilisateur), authentifié avec l'identifiant GitHub déjà enregistré ; dépôt privé `Seydina-s/bic-gouv-sn` créé. | Utilisateur | Demande explicite | Push, PR et CI possibles depuis Claude Code |
| 24/09/2026 | **TypeScript 6.0.3** (et non 7.0) ; outils racine épinglés en version exacte : ESLint 10.11, typescript-eslint 8.70 (règles strictTypeChecked), Prettier 3.9. Le monorepo refuse de s'installer sous Node < 24.3 (`engineStrict`). | Claude (application du stack validé) | typescript-eslint 8.70 n'accepte que TypeScript < 6.1 ; TypeScript 7 (réécriture native) sera adopté quand l'outillage le supportera. | Montée vers TS 7 = tâche de backlog future |
| 24/09/2026 | Modèle de contenu : champs en camelCase côté TypeScript, en snake_case côté SQL (correspondance dans `docs/data-model.md`). La rubrique d'un article est un identifiant libre validé, pas une liste figée, tant que la cartographie de presidence.sn (Phase 1) n'est pas faite. `sourcePublishedAt` peut être nul, et n'est jamais deviné. Objets stricts : tout champ inconnu est refusé, ce qui déclenche la quarantaine. | Claude (application de CLAUDE.md §4.2, §1) | Conventions JS/SQL ; ne rien inventer sur les rubriques ; détecter les changements de structure de la source | Base de F-03 ; énumération des rubriques possible après la cartographie |
| 24/09/2026 | Couleurs : échelles 50→900 générées en OKLCH, ancrées sur les couleurs exactes de la charte (vert 600, jaune 100, rouge 600) ; jaunes foncés légèrement ambrés. Petit texte vert en thème clair = vert 700 (`#016A31`), car le vert charte `#00853F` n'atteint que 4,41:1 sur fond gris clair (4,5 exigé) ; `#00853F` reste la couleur des boutons et barres. Jaune sur barre verte : grands textes et icônes uniquement (3:1). Thème sombre : vert 400 pour les actions, vert 800 pour les barres. | Claude (application charte + WCAG AA, §1) | Contraste AA vérifié par test dans les deux thèmes | 42 paires couleur/fond contrôlées automatiquement ; toute régression bloque les tests |
| 24/09/2026 | Configuration Vitest mutualisée à la racine (`vitest.shared.ts`), Vitest installé à la racine. | Claude | Zéro duplication entre packages | Chaque package réutilise le même seuil de couverture (80 %) |
| 24/09/2026 | i18n : socle maison léger (sans i18next) — clés typées à la compilation, français = catalogue de référence et de repli, règles de pluriel FR/WO écrites à la main (Intl.PluralRules non garanti sous Hermes), interpolation `{nom}`. Langue : celle choisie à l'onboarding, sinon wolof si le téléphone le propose avant le français, sinon français. | Claude (application §1, §10) | 2 langues seulement : zéro dépendance, aucun poids ajouté à l'app, faute de frappe sur une clé = erreur de compilation | Adaptateur React (hook) ajouté avec l'app mobile (F-07) |
| 24/09/2026 | Catalogue wolof de l'interface livré **vide** : aucun texte wolof n'est rédigé par Claude. Repli visible sur le français, et liste automatique des textes à traduire. Les textes wolof viendront de locuteurs natifs, puis seront relus. | Claude (règle « ne jamais inventer », §10) | Exactitude et respect de la langue | Question ouverte : qui rédige/valide le wolof de l'interface ? |
| 24/09/2026 | Résilience : implémentation maison sans dépendance (timeout, retry « full jitter », circuit breaker à 3 états, composition `createResilientCall`). Le retry exige `idempotent: true` explicite ; un circuit ouvert ou une annulation ne sont jamais retentés ; chaque essai passe par le disjoncteur avec son propre délai. Codes d'erreur `RESILIENCE_*` destinés au catalogue d'erreurs (F-11). | Claude (application §4.5) | Fonctionne à l'identique sous Node et Hermes (pas d'`AbortSignal.any/timeout`) ; comportement entièrement testé avec horloge simulée | Base obligatoire de tout appel externe (source, Firecrawl, LLM, TTS, push, tuiles) ; état des disjoncteurs exposable dans l'admin |
| 24/09/2026 | App mobile : Expo SDK 57 (React Native 0.86.3, React 19.2.3, Hermes, New Architecture), Expo Router (`src/app`), `userInterfaceStyle: automatic` + expo-system-ui (thème qui suit le téléphone en direct), orientation libre (tablettes, pliables), expo-localization pour la langue du téléphone. Tests mobiles avec jest-expo + React Native Testing Library 14. Consignes Expo pour agents (`apps/mobile/AGENTS.md`) conservées ; activation automatique d'un plugin Claude du modèle retirée (§9 : pas d'installation sans validation). | Claude (application du stack validé) | Versions imposées par le SDK 57 via `expo install` | React doit rester en 19.2.3 dans tout le monorepo (admin Next.js inclus) : deux versions de React = plantage |
| 24/09/2026 | pnpm 12 : protection « âge minimum de publication » conservée ; exceptions ajoutées par `expo install` uniquement pour les paquets officiels Expo du SDK 57 (`minimumReleaseAgeExclude`). | Claude | Protection contre les paquets piégés tout juste publiés, sans bloquer le SDK officiel | À revoir à chaque mise à jour du SDK |
| 24/09/2026 | API : Fastify 5 + fastify-type-provider-zod 7 (schémas Zod partagés = validation + OpenAPI 3.1 générée), `GET /v1/health` (no-store), `GET /v1/openapi.json`, documentation interactive `/docs` **hors production uniquement**, erreurs JSON uniformes `{code, message, requestId}` sans détail interne, en-tête `x-request-id` pour tracer un incident, configuration par variables d'environnement validées, arrêt gracieux (SIGTERM/SIGINT, délai max). Build : tsdown (un fichier de 12 Ko, paquets `@bgs/*` inclus). Dev : tsx. | Claude (application §4.1, §4.5) | Stack validé ; aucun secret, aucune fuite d'erreur | Base de toutes les routes `/v1` |
| 24/09/2026 | pnpm : seul `esbuild` (compilateur reconnu, requis par tsx/tsdown) est autorisé à exécuter un script d'installation (`allowBuilds`). | Claude | Tout autre script reste bloqué par défaut (sécurité de la chaîne d'approvisionnement) | Toute nouvelle autorisation passe par decisions.md |
| 24/09/2026 | Audit : 2 vulnérabilités modérées dans les dépendances d'Expo, **risque accepté temporairement**. `uuid` < 11.1.1 (outil de build `xcode`, absent de l'app : sans impact). `decode-uri-component` 0.2.2 via `query-string` 7 d'expo-router : un lien volontairement malformé pourrait figer l'app de la personne qui l'ouvre ; le correctif (0.5, ESM uniquement) est incompatible avec `query-string` 7, le forcer risquerait de casser la navigation. | Claude — à confirmer par l'utilisateur | Impact limité à l'appareil de la personne ; correctif forcé plus risqué que le défaut | Suivi SEC-01 à chaque mise à jour du SDK Expo |
| 24/09/2026 | Skill **Impeccable** 4.x installé dans le projet (`.claude/skills`, `.claude/agents`), **sans le hook automatique** (qui téléchargerait et exécuterait un programme à chaque modification) ; binaire du moteur exclu de Git. 21st.dev : parcours gratuit du site uniquement (pas de clé API). | Utilisateur | Rendu d'interface haut de gamme, sans surcoût | Skills de design chargés avant toute tâche visuelle |
| 24/09/2026 | Audit des tokens (Impeccable + ui-ux-pro-max) : 11/20 → corrections appliquées (voir `docs/design/audit-tokens-2026-09-24.md`). Rôles typographiques à hauteur de ligne absolue, une police par graisse, classes de fenêtre Material (600 / 840), nuances sombres tonales, rôles conteneur/tertiaire/voile, ressorts d'animation. Pas de Dynamic Color. Polices provisoires chargées (6 graisses, glyphes wolof vérifiés). | Utilisateur (validation de l'audit et des corrections) | Qualité visuelle, accessibilité, pliables | Base prête pour les premiers écrans |
| 24/09/2026 | `PRODUCT.md` (fiche produit Impeccable) créé à la racine à partir de CLAUDE.md et des réponses de l'utilisateur : admin utilisé sur ordinateur de bureau et portable, par les équipes communication, direction et technique ; aucune identité visuelle officielle connue (ne jamais imiter un emblème officiel). | Utilisateur | Contexte produit obligatoire avant tout travail visuel | Base de toutes les futures décisions de design |
| 24/09/2026 | L'univers visuel de la marque (tour de directions Impeccable, puis DESIGN.md) sera choisi **au premier écran citoyen** (Phase 2) ; l'admin en héritera. F-09 reste une coquille sobre, en mode « Operate ». | Utilisateur | La marque se joue d'abord dans l'app citoyenne | Pas de DESIGN.md avant la Phase 2 |
| 24/09/2026 | Admin : Next.js 16 (App Router, Turbopack) + React 19.2.3 (même version que le mobile) + Tailwind CSS v4 (compatible avec les composants 21st.dev) branché sur les tokens `--bgs-*`. Thème qui suit le système, polices auto-hébergées (next/font), icônes Phosphor, en-têtes de sécurité (anti-iframe, nosniff, no-referrer), non indexé. Chaînes dans un catalogue dédié `adminFr` (FR seul). Sonde d'état : délai 3 s + 1 nouvelle tentative, **sans disjoncteur** (l'écran doit toujours montrer l'état réel). | Claude (application du stack validé + réponses utilisateur) | Écran utile dès maintenant : « le service fonctionne-t-il, et que faire sinon » | Base de la supervision (Phase 4) |
| 24/09/2026 | CI GitHub Actions : 3 jobs parallèles (qualité : format/lint/types/tests ; builds de production : API, admin, bundles mobiles Android + iOS ; sécurité : gitleaks sur tout l'historique + `pnpm audit` bloquant au niveau « high »). Actions épinglées par empreinte (SHA), permissions minimales (lecture seule ; `pull-requests: read` pour gitleaks uniquement), runner figé `ubuntu-24.04`, action d'installation commune. Dependabot hebdomadaire (npm groupé Expo/outillage, GitHub Actions). | Claude (application §4.1, §4.5) | Reproductibilité et sécurité de la chaîne d'approvisionnement | Aucun code n'entre dans `main` sans CI verte (protection de branche à activer) |
| 24/09/2026 | Gitleaks : gratuit pour un compte personnel ; **licence gratuite requise** si le dépôt passe un jour dans une organisation GitHub (ex. compte du BIC). | Claude | Conditions de gitleaks-action | À prévoir lors d'un transfert du dépôt |
| 24/09/2026 | Catalogue d'erreurs : source unique `packages/shared-types/src/errors/error-catalog.ts` (quoi, où, impact, gravité rouge/jaune/vert, que faire), type `ErrorCode` imposé à tout le code (résilience, API, admin) : un code non catalogué ne compile pas. `describeError()` signale tout code inconnu reçu à l'exécution (« non répertorié ») au lieu de le masquer. `docs/errors-catalog.md` est **généré** (`pnpm errors:doc`) et un test vérifie qu'il est à jour. L'admin affiche les explications du catalogue et le code. | Claude (application §4.5) | Zéro duplication, explications identiques partout | Chaque nouvelle erreur passe par le catalogue |
| 25/09/2026 | Sentry (offre gratuite) : API `@sentry/node` 10.75 (ligne 11.0 sortie la veille écartée), chargé avant le serveur via `node --import ./dist/instrument.mjs` (méthode ESM officielle), erreurs 5xx remontées via `setupFastifyErrorHandler` ; mobile `@sentry/react-native` 7.11 (version d'Expo SDK 57), `Sentry.wrap` seulement si actif, sessions suivies (taux sans crash). **Désactivé tant qu'aucun DSN n'est fourni** (`SENTRY_DSN`, `EXPO_PUBLIC_SENTRY_DSN`). Données personnelles exclues : pas d'IP, pas de cookies, pas de capture d'écran ni de hiérarchie de vues. Chaque rapport est étiqueté avec son code du catalogue (`error.code`, `error.catalogued`). `@sentry/cli` autorisé à exécuter son script d'installation (envoi des source maps). | Utilisateur (offre gratuite) / Claude (mise en œuvre) | Suivi des plantages conforme CDP / loi 2008-12 | Envoi des source maps (lisibilité des plantages) à brancher dès la création du compte (MON-01) |
| 25/09/2026 | Protection de `main` approuvée par l'utilisateur mais impossible en l'état (GitHub : dépôt privé + offre gratuite). Tant qu'aucune décision n'est prise (GitHub Pro 4 $/mois, ou organisation du BIC en offre Team), Claude s'interdit tout push direct sur `main` : fusion uniquement par PR avec CI verte. | Utilisateur (accord) / GitHub (limite de l'offre) | Coût financier = validation utilisateur (§7, §9) | Décision de coût en attente |
| 25/09/2026 | Suite du projet : **tranche verticale** « premiers vrais articles » (quelques articles réels de presidence.sn → API → première liste dans l'app), avant de compléter la Phase 1 (historique, wolof, temps réel). | Utilisateur | Montrer tôt du vrai contenu, sans rien inventer | Nouvelles tâches VS-01 à VS-06 |
| 25/09/2026 | Clôture du Sprint 0 : critère « app vide déployable » **partiellement atteint** : bundles Android + iOS construits en CI, app vérifiée sur téléphone réel via Expo Go (utilisateur). Le build natif installable (EAS) attend l'identifiant officiel de l'app (A-02) : aucun identifiant n'est inventé. | Claude (constat) | Un identifiant d'app suit l'app jusqu'aux stores | Build EAS dès A-02 réglé |
| 25/09/2026 | **Collecte presidence.sn via son API JSON publique** (`bo-admin.presidence.sn/api/front`, la même que le site officiel) plutôt que par lecture des pages avec Firecrawl. Firecrawl reste prévu pour e-senegal.sn et comme repli, derrière la même interface `SourceProvider`. Règles : au plus 1 requête/s, User-Agent identifiant le projet, attribution et lien vers la source. Accord écrit du BIC demandé (L-02). | Utilisateur | Données exactes et structurées, liaison FR↔WO fournie, gratuit, insensible aux changements de mise en page | Dérogation au stack §4.1 (outil d'ingestion) ; VS-02 démarre |
| 25/09/2026 | Modèle de contenu (DM-01) : `sourcePublishedAt` remplacé par `sourcePublishedOn` (date calendaire, presidence.sn ne donne pas d'heure) + `sourceUpdatedAt` (horodatage du back-office). Identifiant d'article = UUID v5 dérivé de l'identifiant d'article de base de la source (commun FR/WO) : collecte idempotente. Premier jet de la tranche : articles sans images ni PDF (pipeline média MED-01). | Claude (règle « ne rien inventer ») | Aucune heure inventée ; pas de doublon au re-passage | Stockage et API s'appuient sur ces champs |
| 25/09/2026 | **Passage en mode autonome** (CLAUDE.md §7), à la demande explicite de l'utilisateur : poursuivre les tâches sans validation une à une, prendre la meilleure décision à sa place et la consigner ici, sauter les blocages qui exigent une action personnelle et les récapituler, s'arrêter seulement face à un blocage incontournable. Garde-fous maintenus : aucun coût engagé, aucune action irréversible, aucune publication hors de l'app. | Utilisateur | Accélérer la tranche verticale | Auteur des décisions suivantes : « Claude (autonome) » |
| 25/09/2026 | Stockage provisoire (VS-03) : fichier JSON unique réécrit de façon atomique et revalidé à chaque lecture, derrière l'interface `ArticleRepository` ; PostgreSQL le remplacera en Phase 1 sans toucher au reste. Toute modification incrémente la version et conserve l'ancienne. Une version wolof arrivée plus tard est fusionnée dans le même article ; le français reste la langue d'origine quand il existe. Pas de base hébergée (Neon…) avant le chiffrage de l'hébergement (S1-02). | Claude (autonome) | Avancer la tranche sans engager de coût ni de choix d'hébergeur | Base de données réelle = Phase 1 |
| 25/09/2026 | API actualités (VS-04) : le corps des articles est converti **côté serveur** en blocs structurés (paragraphe, titre, liste, citation, image ; gras/italique/souligné/lien HTTPS). L'app les affiche avec ses composants natifs : aucun HTML ni web view sur le téléphone. Extrait = premiers mots du texte, coupés sur un mot, jamais réécrits. Cache : `max-age=60, stale-while-revalidate=600`, ETag et 304. | Claude (autonome) | Sécurité (rien d'exécutable côté app), cohérence du design, légèreté réseau | Contrat partagé `packages/shared-types/src/api/news.schema.ts` |
| 25/09/2026 | **Univers visuel de la marque (VS-05) : « le pagne tissé »** (tissage en bandes manjak/wolof). Chaque article est une bande, l'actualité du jour forme « la pièce du jour ». La lisière de chaque bande porte un motif propre à la rubrique (chevron, points, sergé, échelle, losange, tiret, grille), ce qui évite de ne coder l'information que par la couleur. Fond blanc (sombre : noir indigo), date du jour en grand, baobab une seule fois par vue. Tirage Impeccable n°5/7 (clé 9db7b8bb) ; ajouts retenus : repères sans mots, marque « là où vous vous êtes arrêté », hiérarchie par la graisse avant la taille, baobab comme unique repère. Contrat : `apps/mobile/.impeccable/surfaces/`. | Claude (autonome), par délégation de l'utilisateur | Univers ancré dans la culture sénégalaise, lisible, léger sur les téléphones modestes, sans imiter d'emblème officiel | DESIGN.md sera rédigé à partir du rendu réel |
| 25/09/2026 | App (VS-06) : données avec TanStack Query + cache persistant **AsyncStorage** (au lieu de MMKV, absent d'Expo Go, qui sert aux tests sur téléphone) ; liste FlashList ; images expo-image ; icônes Phosphor (react-native-svg) ; 5 onglets de la maquette, ceux qui ne sont pas encore construits affichent un état « bientôt disponible » honnête ; adresse de l'API par `EXPO_PUBLIC_API_URL`. | Claude (autonome) | Tester sur téléphone avec Expo Go sans build natif ; hors ligne dès maintenant | MMKV quand les builds natifs (EAS) existeront |
| 25/09/2026 | VS-06 : animation d'entrée des bandes avec l'`Animated` natif de React Native (moteur natif) au lieu de Reanimated (−0,9 Mo de bundle, 6,3 → 5,4 Mo) ; mouvement supprimé si « réduire les animations » est actif. Compteur d'en-tête limité aux publications **du jour** (sinon « Dernières actualités ») pour ne rien laisser croire de faux. Colonne de lecture centrée sur grand écran en attendant les deux volets (RESP-01). react-native-web ajouté pour vérifier le rendu sur PC (non livré dans l'app). | Claude (autonome) | Performance sur entrée de gamme, exactitude, lisibilité | Validation finale sur appareils (QA-02) |
| 25/09/2026 | Durcissement API : limite **généreuse** de 600 requêtes/minute/adresse (les opérateurs mobiles sénégalais regroupent beaucoup d'abonnés derrière une même adresse — CGNAT ; le CDN absorbera les pics), sondes de santé jamais limitées, réponse 429 `RATE_LIMITED` ; en-têtes helmet, CSP `default-src 'none'; frame-ancestors 'none'` en production ; sonde de disponibilité `/v1/health/ready` (503 `SERVICE_NOT_READY` si le stockage est illisible). Admin : CSP stricte hors scripts inline de Next.js (nonce : SEC-03). | Claude (autonome) | Sécurité sans bloquer de citoyens légitimes | Seuil ajustable par `RATE_LIMIT_PER_MINUTE` |
| 25/09/2026 | Détection temps réel : sondage adaptatif de la page 1 FR et WO (30 s après une publication, 1 min de 7 h à 23 h à Dakar, 5 min la nuit) ; seuls les articles nouveaux ou modifiés (`updated_at` de la source) sont récupérés en détail ; délai de détection mesuré pour le SLO < 2 min. | Claude (autonome) | Fraîcheur < 2 min en restant poli avec la source (≈ 2 requêtes par passage) | À remplacer par un webhook si le BIC l'accepte (L-02) |
| 25/09/2026 | Retour utilisateur : la direction visuelle « pagne tissé » ne le convainc pas. Elle reste en place en attendant son choix parmi 2-3 directions (D-05) ; aucune nouvelle décision visuelle structurante n'est prise d'ici là. | Utilisateur | Le design doit plaire à son commanditaire | D-05 prioritaire au retour de l'utilisateur |
| 25/09/2026 | Budget de poids de l'app (S1-03) : **plafond 6 Mo par bundle JavaScript Hermes**, bloquant en CI ; **cible 4 Mo** après allègement (Sentry replay, Zod : PERF-03). Mesure actuelle : Android 5,16 Mo, iOS 4,88 Mo. | Claude (autonome) | Démarrage rapide sur Android 2 Go en 3G sans bloquer le travail en cours | Toute régression au-delà du plafond fait échouer la CI |
| 25/09/2026 | Photos (MED-01) : on conserve l'original de presidence.sn et on publie des variantes **480 et 960 px** en AVIF, WebP et JPEG, chacune à la qualité la plus basse dont la fidélité SSIM ≥ 0,98 ; l'app mobile charge le **WebP** (décodage rapide sur entrée de gamme ; AVIF réservé au web/admin) à la plus petite largeur suffisante pour l'écran. Une photo en échec n'empêche jamais la publication du texte (erreur « info » MEDIA_PROCESSING_FAILED, nouvelle tentative au passage suivant). La couverture de la version principale (FR) n'est jamais remplacée par celle d'une autre langue. Stockage local `.data/media` en attendant le stockage objet + CDN (`MEDIA_BASE_URL`). | Claude (autonome) | Qualité visuelle équivalente, poids ÷ 3 à ÷ 7, zéro coût | Photo visible dans le fil et l'article ; S1-02 (hébergement) fixera le CDN |
| 25/09/2026 | Cache hors ligne de l'app versionné par une **empreinte automatique** des schémas d'API partagés : toute évolution du format invalide l'ancien cache au lieu de risquer un plantage. | Claude (autonome) | Stabilité après chaque mise à jour | Après une mise à jour qui change le format, l'app recharge ses données une fois |
| 25/09/2026 | Contrat d'API **additif** : champs inconnus ignorés, l'app lit les réponses avec un lecteur tolérant (blocs/formats inconnus écartés, article illisible sauté). Aucune suppression ni changement de sens d'un champ sans nouvelle version `/v2`. | Claude (autonome) | Les versions installées de l'app vivent des mois : l'API ne doit jamais les casser | Constaté sur l'iPhone de l'utilisateur (faux « Hors ligne ») |
| 25/09/2026 | Dépôt rendu **public**, de façon sécurisée : l'adresse e-mail personnelle est retirée de tout l'historique (remplacée par l'adresse anonyme GitHub, aussi pour les futurs commits de ce dépôt) ; comme GitHub conserve les anciens commits des pull requests fermées, l'ancien dépôt devient une archive **privée** (`bic-gouv-sn-archive`) et un nouveau dépôt `bic-gouv-sn` reçoit l'historique nettoyé. Réécriture d'historique et push forcé autorisés exceptionnellement pour cette opération. | Utilisateur | Transparence (dépôt public) sans exposer de données personnelles | Protection de `main` désormais possible gratuitement (CI-01) ; PR #7 recréée |
| 25/09/2026 | Direction visuelle (D-05) : **« La Une »** (écran 3 de la planche) combinée aux **motifs du pagne tissé** (écran 1) : bandeau tricolore, en-tête « journal » avec nom et date, grande photo pleine largeur, titres en serif **Literata** (OFL, glyphes wolof vérifiés), carte « Dernier Conseil des ministres » tissée de chevrons, liste avec vignettes à droite, lisière et pastille tissées par rubrique. **Barre de navigation flottante en verre dépoli** : flou réel sur iOS, version translucide sans flou sur Android (coût GPU sur entrée de gamme), lisibilité AA garantie par test même sur photo noire ou blanche. | Utilisateur | Choix de l'utilisateur après la planche de 5 directions et ses captures iPhone | Remplace « pagne tissé seul » ; DESIGN.md (DS-01) à régénérer après validation sur téléphone |
| 25/09/2026 | API : filtre `category` sur `/v1/news` (la carte Conseil des ministres affiche toujours le dernier, même ancien ; sert aussi aux filtres par rubrique prévus en P0). | Claude (autonome) | Exactitude de la carte sans charger tout l'historique | Évolution additive du contrat |
| 25/09/2026 | Dépôt public sécurisé : jeton des automatisations en lecture seule, actions autorisées limitées (GitHub + pnpm + gitleaks) et épinglées par empreinte, approbation manuelle avant d'exécuter le code d'un contributeur externe, analyse des secrets avec blocage à l'envoi, alertes et correctifs Dependabot, signalement privé des failles (SECURITY.md), propriétaire obligatoire en revue (CODEOWNERS), règles sur `main` (pull request obligatoire, contrôles verts, ni push forcé ni suppression). **Aucune licence libre** pour l'instant (tous droits réservés par défaut) : à décider avec le BIC. | Utilisateur (autorisation) / Claude (réglages) | Transparence sans exposer le projet | Licence : question ajoutée à L-02 |
| 25/09/2026 | Dépendances : les versions choisies par le SDK Expo (expo, react-native, React, Jest, Sentry RN) ne sont montées qu'avec une montée de SDK complète (`expo install --fix`) ; Dependabot ne propose plus pour elles que des correctifs. Types Node alignés sur Node 24. PR Dependabot #1-#5 fermées avec explication. | Claude (autonome) | Un React différent de celui du moteur react-native fait planter l'app au démarrage sans que les tests le voient | Tâche UPG-01 |
| 25/09/2026 | Images du texte : stockées chez nous comme les couvertures ; seules les images hébergées par une source officielle sont affichées (les images collées depuis des réseaux sociaux exposeraient les lecteurs à des tiers) ; l'ancienne adresse d'images `bo.presidence.sn` (certificat TLS invalide, jamais contourné) est lue sur `bo-admin.presidence.sn`, qui sert les mêmes fichiers. | Claude (autonome) | Autonomie vis-à-vis de la source, sécurité et vie privée des lecteurs | 13 petites images (émojis Facebook) non affichées |
| 25/09/2026 | Onglet actif : libellé en couleur de texte principale (et non vert de marque) ; l'état se lit à la pilule verte, à l'icône pleine et à la graisse. Poids de l'app : les traductions des messages d'erreur de Zod (64 langues, jamais montrées aux citoyens) sont retirées du paquet via Metro. | Claude (autonome) | Lisibilité AA garantie sur le verre ; −230 Ko pour les téléphones d'entrée de gamme | Aucun changement visible hormis la couleur du libellé actif |
| 26/09/2026 | Vidéos officielles : seules les vidéos YouTube intégrées par presidence.sn sont acceptées (adresse normalisée sur youtube-nocookie.com) ; l'app n'intègre pas de lecteur : une carte « Regarder la vidéo » ouvre YouTube quand la personne la touche. | Claude (autonome) | Poids et fluidité sur téléphone modeste ; aucune connexion à un tiers sans action de la personne | Les interviews vidéo ne sont plus écartées |
| 26/09/2026 | Recherche : version provisoire dans l'API (parcours du stockage, accents et casse ignorés, tous les mots requis, titres en premier), remplacée par Meilisearch avant la mise en production, sans changer la route ni l'app. Les nombres comptent comme mots (« Conseil du 3 septembre »). | Claude (autonome) | Offrir la recherche P0 dès maintenant sans service supplémentaire | SEARCH-01 |
| 26/09/2026 | Méthode : chaque écran, étape et fonctionnalité est conçu en se référant aux applications qui le font le mieux au monde aujourd'hui (références citées). Splash screen en motion design (Rive), à partir du logo que fournira l'utilisateur ; outil externe autorisé si nécessaire. | Utilisateur | Viser une expérience premium et immersive | Benchmark ajouté à chaque tâche d'interface |
